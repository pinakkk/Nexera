{
  "scripts": {
    "api": "test -x apps/api/.venv/bin/python && apps/api/.venv/bin/python -m uvicorn app.main:app --reload --env-file apps/api/.env --port 8000 || (echo 'Missing apps/api/.venv/bin/python. Create the API venv with Python 3.11 first.' && exit 1)",
    "web": "cd apps/web && npm run dev",
    "dev": "make dev"
  }
}
